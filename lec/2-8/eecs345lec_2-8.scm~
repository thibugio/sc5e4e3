#lang racket

(define factorial
  (lambda (n)
    (if (zero? n)
        1
        (* n (factorial (- n 1))))))

; tail recursion with accumulator-passing style
(define factorial-acc
  (lambda (n acc)
    (if (zero? n)
        acc
        (factorial-acc (- n 1) (* n acc)))))